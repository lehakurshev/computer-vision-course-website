server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;  # Обработка маршрутов React
    }

    error_page 404 /index.html;  # Перенаправление на index.html при 404
}